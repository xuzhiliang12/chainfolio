# 备份与恢复

最重要的文件是 `data/state.json`。它不包含助记词或私钥，但包含公开地址的归属关系、资产快照、钱包备注和活动记录。

Windows 运行：

```powershell
.\scripts\backup.ps1
```

备份生成在 `backups`。建议再复制一份到由你控制的加密磁盘，但不要上传公共网盘或 GitHub。

恢复前先停止服务，把当前 `data` 改名留存，然后从备份中取出完整 `data` 目录放回安装目录。恢复后登录并抽查负责人、手机、钱包、地址、活动和资产记录。
